{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Test JSDOM",
            "program": "${workspaceFolder}/server.mjs",
            "runtimeArgs": [
                "--experimental-modules"
            ]
        }
    ]
}
